import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const Login = (props) => {
  return (
    <Fragment>
      <h1>Log In</h1>
    </Fragment>
  );
};

Login.propTypes = {};

export default Login;
